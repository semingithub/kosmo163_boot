<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.semin.app.board.notice.NoticeMapper">
	<select id="list" resultType="NoticeDTO" parameterType="Pager">
	<![CDATA[SELECT * FROM
		(SELECT ROWNUM AS R, T.* FROM 
		(SELECT * FROM TB_NOTICE ORDER BY NOTICE_NO DESC) T)
		WHERE R BETWEEN #{first} AND #{last}]]>
	</select>
	
	<select id="getCount" resultType="Long">
		SELECT COUNT(NOTICE_NO) FROM TB_NOTICE
	</select>

	<select id="detail" resultType="NoticeDTO">
		SELECT * FROM TB_NOTICE
		WHERE NOTICE_NO = #{noticeNo}
	</select>

	<insert id="create" parameterType="NoticeDTO">
		INSERT INTO TB_NOTICE
		VALUES(NOTICE_SEQ.NEXTVAL, #{noticeTitle}, #{noticeContents}, #{professorNo}, SYSDATE, #{noticeHits})
	</insert>

	<delete id="delete" parameterType="NoticeDTO">
		DELETE TB_NOTICE
		WHERE NOTICE_NO = #{noticeNo}
	</delete>

	<update id="update" parameterType="NoticeDTO">
		UPDATE TB_NOTICE
		SET NOTICE_TITLE = #{noticeTitle},
		NOTICE_CONTENTS = #{noticeContents},
		PROFESSOR_NO = #{professorNo},
		NOTICE_DATE = SYSDATE,
		NOTICE_HITS = #{noticeHits}
		WHERE NOTICE_NO = #{noticeNo}
	</update>

</mapper>